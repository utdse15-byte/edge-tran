# Changelog

## 0.2.15 — 2026-07-31

追加模式。按你的要求:**输入框里已经有文字时不再检测、不再拦截,译文直接加在后面。**

### 写入契约从"覆盖"改为"追加"

- 0.2.12 的"输入框已有内容 → 译文停在预览、状态栏提示、等你点覆盖或清空"这条分流**整条移除**。现在译文一律追加到已有内容之后,中间空一行。
- **只有插件自己上一段译文会被替换**,而且仅当它原样待在最末尾时。你改中文重译,输入框里始终只有一段英文,不会越堆越多;你一旦编辑了那段英文,它就归你了,下一次译文追加在它后面。
- **附件/图片/mention 不再阻止写入**(你的明确选择)。实现上没有走"全选替换":追加时只把光标收到末尾插入,替换时只选中我们自己那几个字符——**用户内容从不进入任何被替换的区间**,所以附件节点在技术上不被触碰。回归测试实测:带 `data-attachment` 芯片时写入成功且芯片仍在。
- `CLEAR_TARGET_IF_OWNED`(旧译文清理)同步改为**只删自己那一段**。不改的话,追加模式下插件"拥有"整个输入框,这条命令会连你的文字一起删掉——正是零删除原则要防的事。
- 写入失败时不再回滚:追加只增不减,没有可回滚的东西,失败就如实报告并把已插入的内容留在原地,绝不反过来删你的字。

### 实现要点

- `selectTrailingChars()` 按**文本节点字符**倒着走来圈定我们自己的尾巴。这里踩过一个真实的坑:序列化文本会在每个块边界合成一个换行,而那些换行不属于任何文本节点——用序列化长度去圈选会多吃掉用户的字符(实测把"我自己写的内容"啃成了"我自己写的")。单位必须是文本节点字符。
- 分隔空行只在第一次追加时写入。它在 DOM 里是块结构而不是文本字符,替换尾巴时不会被删掉,再插一次就会每翻译一轮多一个空行。
- `<textarea>` 的值不在子文本节点里,所有权比对必须读 `.value`,否则永远匹配不上、每次都追加。
- 审计新增锚点:`selectTrailingChars`/`nodeTextOf`/`appendPlan` 必须存在,且全选 `selectEditorContents(` 的调用点不得超过 2 处(只允许显式"清空输入框"按钮用)。

### Gemini Live 免费额度桥接(新增工具,插件零改动)

Gemini 免费档普通模型只有个位数到 15 次/天,**Live API 模型不限次数**、只限 token。新增 `scripts/gemini-live-bridge.mjs`(`npm run gemini:live`):对插件说 OpenAI Chat Completions,对 Google 说 Live API。插件一行都不用改——manifest 本来就允许 `http://127.0.0.1:*`,真 Key 留在桥接进程里不进插件存储。

可行性建立在两条实测结论上,不是推测:

- Live 模型**接受纯文字输入**,不需要麦克风(三种 Live 模型逐一验证)。
- `outputAudioTranscription` 回传的是**合成语音所依据的那份文本**,不是声波识别。实测占位符 `⟦ZH2EN_…⟧`、URL、反引号命令、`/etc/hosts`、`v2.1.0`、`85%`、空行分段全部原样保留;用插件真实的系统提示词跑,直接产出四字段齐全的严格 JSON。用真实 `lib/translator.js` 端到端验证:0.7–1.2 秒返回,受保护内容零损伤。

模型只有 `gemini-3.1-flash-live-preview` 三项全合格。`gemini-2.5-flash-native-audio-*` 对 JSON 提示词返回空;`gemini-3.5-live-translate-preview` 只吃语音,纯文字输入毫无响应(它是语音翻译模型)。

**会话复用是个反直觉的坑,已按实测放弃**:本以为复用会话能把系统提示词摊销掉,实测同一会话三轮的 prompt token 是 586 / 767 / 1039(历史在累积),而每次新开会话恒定约 586——第二轮起复用就更贵。桥接默认每次请求新开会话(约 90ms 建连),`--reuse-turns N` 保留给确实需要上下文的场景。

### 测试

- writer 与 panel 四个浏览器套件里编码旧覆盖契约的断言全部按新契约重写(不是删掉):用户文字保留、发送内容包含两段、附件存活、所有权清理只删自己那段、人工编辑后追加而非覆盖。
- 新增 8 项追加语义验证:已有文字后追加、重译只换自己那段、连续三次仍只有一段英文、用户改过我们的译文后归属转移、带附件追加且芯片存活、textarea 追加与重译、所有权清理保留用户文字。


## 0.2.14 — 2026-07-30

真实网关联调修复版。此前所有 Provider 测试都靠替换 `globalThis.fetch` 用手写 `Response` 断言,**从未经过真实 HTTP 传输**:分块传输、跨 TCP 包切断的 SSE 事件、切在多字节字符中间的 UTF-8、内容协商、Content-Encoding、重定向、僵死连接、以及"客户端取消流服务端是否感知"全部在覆盖范围之外。本版新增一个自带的 OpenAI 兼容网关模拟器(`scripts/mock-provider.mjs`,112 个场景),让扩展在真实 socket 上跑起来,由此定位并修复 6 个传输层缺陷,以及 1 个状态栏竞态、1 个数组包裹错误体缺陷。

### ① 流式请求的 Accept 头不正确(修复)

- `buildHeaders` 对所有请求固定发送 `Accept: application/json`,包括 `stream: true` 的 SSE 请求。做真实内容协商的网关会以 **406 Not Acceptable** 拒绝,用户只看到"Provider 请求失败（HTTP 406）",完全无从判断原因。
- 修复:流式请求改发 `Accept: text/event-stream, application/json`(保留 JSON 以便网关回落到缓冲响应)。

### ② 流式响应丢失 usage 与 reasoning_tokens(修复)

- SSE 组装器只把 `choices` 拼回 payload,忽略网关在末尾 chunk 上给出的 `usage`。由于**流式预览默认开启**,`result.reasoningTokens` 实际上恒为 null——0.2.9 承诺的"诊断记录与连通测试会显示返回的 reasoning_tokens 用量"在默认配置下从来不生效。
- 修复:任何 chunk 上出现的 `usage` 都被采纳并写回组装后的 payload,流式与缓冲的用量口径一致。不额外发送 `stream_options`,不产生新的兼容风险。

### ③ 流式路径不接受 typed content parts(修复)

- 缓冲路径的 `extractAssistantText` 早已支持 `content: [{type:"text",text:"…"}]`,流式路径只认 `typeof delta.content === "string"`。代理非 OpenAI 模型的中转站普遍用数组形式,于是同一个网关**关掉流式预览能用、开着就报"assistant 文本字段为空"**。
- 修复:新增 `streamedDeltaText`,流式与缓冲共用同一套 content 解析口径;非文本部件仍然产出空串,交由终局校验拒绝。

### ④ 三个可降级字段只有两次降级预算(修复)

- 兼容降级循环上限是 3 次尝试(即最多 2 次降级),但可降级字段有三个:`response_format`、`temperature`、`stream`。上限是流式功能(0.2.8)加入之前定下的——逐项拒绝这三个字段的网关**永远无法被满足**,报"请求参数或模型协议不兼容"且不提示"关掉流式预览即可"。
- 修复:上限放宽到 4 次尝试 / 每字段一次降级。无名 400/422 仍然立刻失败、绝不进入重试循环,预算只对"明确指名字段"的拒绝放宽。

### ⑤ 被标错 Content-Type 的 JSON 响应白白失败(修复)

- 部分网关对所有响应一律标 `text/event-stream`,正文却是普通 JSON。SSE 解析器一个事件都没取到 → 报"Provider 流式响应为空",而完整正文其实就在内存里。
- 修复:流中确实没有任何事件时,尝试把缓冲区当作 JSON 正文解析并复用。**不产生第二次请求**,不重复计费。

### ⑥ 丢失的流式事件被算到模型头上(修复)

- 以 `{` / `[` 开头却解析失败的 `data:` 载荷 = 丢了一个 delta,原先被静默忽略。流仍以 `finish_reason: stop` 收尾,于是拼出来的是**中间缺一块的译文**;校验随后失败并报"模型可能复述了原稿",把传输故障说成模型的错,还要再花一次修复请求。
- 修复:新增 `stream_event_lost` 错误码(带 `droppedEvents` 计数),如实报告传输不完整;面板文案同时给出"可在设置中关闭流式预览改用缓冲请求"的出路。非 JSON 的 keep-alive(`data: ping` 之类)仍然照常忽略;`[DONE]` 改用 trim 比较,避免 `data: [DONE] ` 这种带尾随空白的写法被误判成丢包。

### 工具与测试

- **新增 `scripts/mock-provider.mjs`**:零依赖的 OpenAI 兼容网关模拟器,实现 `/v1/chat/completions`(缓冲 + SSE)、`/v1/responses`(缓冲 + SSE)、`/v1/models`,内置确定性词典翻译模型(遵守 english / back_translation / corrections / ambiguous 契约与占位符规则)。112 个场景各自是一个 Base URL(`http://127.0.0.1:8787/<scenario>/v1`):限流、5xx、nginx HTML、登录页、GBK、gzip、BOM、重定向、socket reset、僵死流、逐字节 SSE、切在多字节字符中间、CRLF + 注释心跳、pretty-print 多行 data、丢事件、超限响应、逐项参数拒绝、思考字段拒绝、协议错配、占位符破坏、越界纠错……`npm run mock:provider` 可直接给真实扩展当本地网关手动联调。
- **新增 `tests/e2e-provider.test.js`(104 项)**:全部经真实 socket 驱动 `lib/translator.js` + `lib/provider.js`。
- **新增 `scripts/panel-live-provider-test.py`**:Chromium 里跑真实 `panel.js`,通过真实 HTTP 打到本地网关,再经真实 writer 链路写入。覆盖流式预览增量落到 UI、限流不写入、丢包文案不冤枉模型、运行期降级不被持久化、SSE 路由故障的连通测试诊断、评审提醒渲染、模型探测,以及**计费行为**:连打一串键只产生 1 次付费请求、重复翻译同一稿 +0、只加尾随空格 +0(服务端计数核对)。
- `npm run check` 此前只检查 `.js`,`.mjs` 被整体跳过(包括 `scripts/audit.mjs` 自己);现已纳入。
- 四个浏览器套件里有两个把 Chromium 路径硬编码成 `/usr/bin/chromium`,`CHROMIUM_PATH` 只在另外两个生效——非 Debian 环境下 `verify:full` 直接跑不起来。现已统一支持环境变量并自动探测常见路径。
- `TranslationValidationError` 的 errors 数组去重:占位符还原前后两层校验会产出同一句话,用户此前会看到"模型返回的 back_translation为空；…；模型返回的 back_translation为空"。
- 审计新增 0.2.14 传输不变量锚点(流式 Accept、`streamedDeltaText`、`stream_event_lost`、`chatUsage`)。
- 新增畸形响应扫描:19 种畸形正文逐一验证只会产出 ProviderError / TranslationValidationError,不会有裸 `TypeError` 冒到面板;响应正文中的 `__proto__` 不能污染 `Object.prototype`。
- 真实 UI 上新增中文输入链路场景:IME 组词全程只 1 次付费请求且发的是完整句子、瞬时 500 自动重试一次即恢复、粘贴 65,000 字超限草稿 0 次付费请求且原文全量保留。
- 新增请求成形矩阵扫描:协议 × 流式 × 回译档 × 思考策略 × 方言 共 252 种组合逐一核对——出厂默认(继承 + 不发送)在"拒绝任何未知字段"的严格网关上全部通过;每种方言只发它自己那几个字段,不适用于当前协议时一个都不发;deepseek 方言按文档移除采样参数。
- 新增首次使用与错配扫描:`javascript:`/`data:`/`file:`/`ftp:`/带口令/明文 http 远程 Base URL 全部拒绝;Key 含换行/制表符/非 Latin-1/超长、模型为空或超长、鉴权 Header 名非法等 12 种情形**一次网络请求都不发**;网关不可达在翻译与 `/models` 上都是 `network_error`;空草稿与未配置 Key 在真实 UI 上都是 0 次付费请求。
- 新增真实 UI 竞态场景:被取代的旧译文永不落到输入框或预览;在途请求遇到另一窗口轮换 Provider 立即作废;Responses 协议 + 独立回译两次请求都打 `/responses` 且第二次不含中文原稿;连续 7 次网关失败后 Key 不出现在诊断/状态栏/DOM/存储任何一处;存储配额写失败时如实报"未能保存"且草稿不丢。

### 计费透明度与错误可读性(补充改进)

- **本次翻译实际发出几次请求现在会显示**。兼容降级与 JSON 格式重试此前完全不可见:一次翻译可能悄悄花掉 3 次付费请求而界面上毫无痕迹。`translateDraft` / `backTranslate` 现在返回 `requestCount`,超过 1 次时写入诊断日志;连通测试的"通过"行也会标注本次测试实际花了几次请求(包含独立回译),这样"这个网关每次都要两步降级"在正式使用前就能看见。
- **连通测试不再吞掉本地预检的原因**。`testTranslationConnection` 此前把所有非 ProviderError 一律压成一句"连接测试失败";鉴权 Header 名非法、鉴权前缀含非 Latin-1 字符这类**请求都还没发出**的失败,唯一可行动的那句话就此丢失。现在保留原因:"连接测试失败:…"。

### ⑦ 信息性状态覆盖掉更新更重要的错误(修复)

- `scheduleExternalConfigurationReload` 有 80ms 防抖、之后还要 `await` 一次存储读取。用户完全可以在这个窗口里做事——比如另一个 Edge 窗口刚清除/轮换了 Key,当前窗口点"翻译并同步"得到"请先配置 API Key"。此时那条**纯信息性**的"已采纳另一个窗口的配置修改,自动流程继续"随后落地,把错误状态整条盖掉:状态栏宣称自动流程在继续,实际这次翻译已经中止,唯一线索只剩设置弹窗被打开了。同一个窗口里的限流冷却提示、写入事故提示也会被同样盖掉。
- 修复:`setStatus` 增加单调计数器;外部配置通知在**排期时刻**取快照,落地时若发现期间有更新的状态就不再覆盖(采纳事实照旧写入诊断日志,信息不丢)。重新读取失败那条 `error` 状态仍然无条件生效——它伴随暂停,属于必须让用户看到的。

### ⑧ 数组包裹的错误响应体导致整套错误信息失效(修复)

用真实的 Google AI Studio OpenAI 兼容端点(`https://generativelanguage.googleapis.com/v1beta/openai`)联调时发现:该网关把**每一个**错误都包在一个单元素 JSON 数组里——`[{"error":{"code":404,"message":"models/… is not found …"}}]`,而且 `error.code` 是数字、没有 `error.param`、Key 无效返回 400 而非 401、429 不带 `Retry-After` 头。

按对象读取时 `error.code` / `error.param` / `error.message` 全是 `undefined`,连锁后果:

- **模型 ID 写错被报成"端点不存在,请检查 Base URL"**——把用户支到唯一没错的地方去改。兼容层的 `/models` 还查不到 `gemini-3.5-flash`(必须手填),所以这条路径很容易踩到。
- 网关自己那句最有用的话全被丢弃(配额还剩多少、哪个参数非法、"Please pass a valid API key")。
- **兼容降级永远不可能触发**:`unsupportedFields` 来自 `error.param`/`error.message`,两者皆空 → `compatibilityHint` 恒为 false。
- 思考参数被拒同样识别不到,退化成笼统的参数不兼容。

修复:新增 `unwrapErrorEnvelope`,对数组包裹的错误信封取出第一个带 `error` 的元素(HTTP 200 携带同款信封时一并处理)。顺带修正两处配套问题:模型失败识别的 `\bmodel\b` 放宽为 `\bmodels?\b`(Google 说的是"**models**/x is not found",单数匹配不上);`incompatible_request` 的文案补上网关消息摘录——Google 对无效 Key 返回的是 400 而不是 401,不带上那句"Please pass a valid API key"用户根本无从判断。

真实端点复验:改后"模型 ID 写错"正确归类为 `model_not_found` 并带出 Google 原文;正常翻译、占位符保全、流式预览均正常。

### 复核确认无问题的行为(不改动)

真实网关下逐项确认符合设计:401/403 不重试且不回显 Key(即使网关把 Key 原样写进错误正文);429 的 `Retry-After` 秒数与 HTTP-date 都能解析,冷却下限 3 秒、上限 24 小时;5xx 与普通 400 在 Provider 层不重试;超限响应在有无 `Content-Length` 两种情况下都被截断,无尽流被取消且服务端确实观察到断开;重定向按 `redirect: "error"` 拒绝;GBK 乱码不会被当成合法译文;模型列表上限 5000 并去重清洗;超大错误正文仍按 HTTP 状态分类;`error: null` 不误判为逻辑错误;同时带可用 choices 与 error 对象的 200 照旧拒绝;Windows/`~`/`$VAR` 路径、emoji 与星平面字符、字面占位符文本全部原样保留;占位符还原后超 100,000 字符按设计硬失败。

## 0.2.13 — 2026-07-20

外部定位分析核验修复版。用户提供了另一份 AI 对定位不稳定的代码级分析(基于 main@0.2.11);逐条对照当前代码核验,5 项发现中 4 项确认属实并修复,1 项(BIND_RESULT 竞态)确认为低危、采取轻量缓解。

### ① 导航后旧输入框粘滞(确认属实,修复)

- 原逻辑在 URL 变化后只要旧节点仍连接且可编辑就直接沿用,不判断它是否属于当前页面;之后的候选重排滞回(+60 分且文本相等)又会永久阻止切换——旧路由节点被缓存/隐藏保留时,面板显示"已定位"但实际绑定上一页的节点。
- 修复:导航后**发现优先**——总是先重新定位;SPA 真复用同一节点时定位返回同节点并走原有对账分支;旧节点只在"定位无果且通过深可见性检查"时才作为回退;两者皆无 → 如实上报未就绪。稳态滞回(+60)按分析建议保留,防抖动语义不变。

### ② 可见性判断缺祖先检查(确认属实,修复)

- `isVisible` 只查自身 computed style:祖先 `opacity:0`、`inert`、`aria-hidden="true"` 全部漏过(display/visibility 可继承或压扁矩形,不受影响)——被路由缓存隐藏的旧输入框因此仍被当作可写目标,是①的放大器。
- 修复:候选编辑器改用**深可见性检查**(祖先链逐层查 hidden/inert/aria-hidden/display/visibility/opacity<0.01/content-visibility);发送按钮等旁证保留浅检查以控制每秒扫描成本。写入前安全检查复用同一函数,自动获得增强。
- 未采纳 elementsFromPoint 命中测试:悬浮层(建议弹窗、工具条)合法遮挡输入框会造成误判不可用,弊大于利;记录为不采纳。

### ③ 隐藏超时误报"被外部清空"(确认属实,修复)

- 5 秒宽限到期且找不到替代时,`bindComposer(null)` 对仍有文字的旧节点调用 `confirmClear` → 伪 TARGET_CLEARED,面板宣布"输入框被外部清空"而文字明明还在。
- 修复:引入"不可用≠被清空"语义——旧节点仍连接且文字原样存在时,只上报 `composer_unavailable`(composerReady=false,禁止写入),**保留所有权与发送意图**;重新可见后自动重认领(pluginOwned 延续)。只有实际观察到文字消失才可能产生 TARGET_CLEARED/SEND_CONFIRMED。

### ④ 会话内联编辑器软惩罚不彻底(确认属实,修复)

- article/conversation-turn 内的编辑器原来只扣 90 分,带完整语义+提示+发送按钮时仍可能过 55 分门槛,自动写入历史消息编辑框。
- 修复:自动定位对其**硬排除**(NEGATIVE_INFINITY);手动绑定保留为显式逃生通道。

### ⑤ BIND_RESULT 携带 ATTACH 前旧状态(确认属实,低危,轻量缓解)

- SW 发送 ATTACH 后立即用 `writer.lastState` 应答,面板短暂显示"未找到输入框",随后被 attach 后的 WRITER_STATE 纠正。确为竞态,但只造成一次瞬时文案闪烁。
- 缓解:面板改为"已绑定,正在定位输入框…"。分析建议的 ATTACH/BIND_RESULT 握手协议改造**明确不采纳**:改动波及租约状态机,收益仅是消除一条过渡文案,风险收益不成比例。

### 测试

- writer 安全套件新增三组回归:祖先隐藏的旧路由节点 + 新同构空输入框共存 → 必须切换到新节点、旧文字原样保留、全程无伪 TARGET_CLEARED/SEND_CONFIRMED;祖先隐藏超过宽限期且文字未动 → 只报未就绪,重新可见后所有权延续;article 内全证据编辑器 → 永不自动绑定。

## 0.2.12 — 2026-07-20

零删除与定位稳定版。针对两条反馈:「同步时总想删掉我输入框里已有的内容」「切页后之前定位到的输入框找不到」。

### 自动流程零删除

- **移除"旧译文自动清理"**:修改中文不再触发对 Claude 输入框的任何自动清空(该功能连同设置项一并退役,旧存储中的开启状态被显式忽略)。插件自己的旧译文由下一次同步直接覆盖;非插件内容自动流程永远不碰。
- **中途换稿不再闪空**:写入返回时中文已变化的场景,旧英文原地保留等新译文覆盖,不再先清空后重写。
- **输入框已有内容改为安静处理**:绑定/刷新时发现非插件内容,不再弹"检测到人工修改"横幅、不暂停——静默进入保护态,新译文停在预览,状态栏说明去向。横幅只保留给真正的冲突(插件文本被人工实时修改、写入回滚失败)。
- **新增"清空输入框"按钮**(确认后执行):唯一可以删除非插件内容的路径。writer 新增 CLEAR_TARGET_FORCE 命令——除所有权检查外,状态校验/可见性/附件保护/中断回滚/回读校验与普通清理完全一致;含附件时照样拒绝。
- writer 端零删除契约不变并新增测试锚定:非强制写入拒绝覆盖非插件内容;CLEAR_TARGET_IF_OWNED 只清插件原文。

### 定位稳定性(切页后找不到输入框)

- **根因**:候选评分的 fail-closed 门槛假设输入框贴近视口底部;claude.ai 首页/新会话的**居中**输入框在提示属性缺失/迟到、发送按钮未输入前隐藏时被永久排除——切页到该路由后怎么等都定位不上。
- 新增"居中主输入框"形状识别(强编辑器语义 + 宽度/中带位置 + 不在会话消息内),门槛与惩罚项同步调整;会话内的行内编辑框仍被结构性排除,fail-closed 语义保持。
- 新增 `visibilitychange`/`focus` 重扫触发:后台标签页计时器被节流后,切回即刻重定位。
- REQUEST_WRITER_STATE 在输入框未定位时兼作重定位触发:面板每次询问状态都推动一次发现。

### 测试

- writer 安全套件新增:居中无提示输入框可定位、非强制写入拒绝外来文本、强制清空成功/附件拒绝。
- panel 状态机套件新增零删除场景:外来文本安静采纳(无横幅/不暂停)、自动同步不删除、显式清空按钮全链路;"改中文自动清空旧译文"的旧断言按新契约反转(断言零清理)。

## 0.2.11 — 2026-07-20

摩擦清除与可诊断化版。针对三条实际使用反馈:「/v1/responses 永远 502 但 chat 正常」「暂停/恢复太烦」「有时不自动同步(疑似确认提示)」。写入侧安全机制(租约、发送检测、fail-closed 写入)零改动。

### /v1/responses 502 可诊断化

- HTTP 错误现在保留网关证据:错误正文摘录(remoteMessage,脱敏——控制字符清除、`sk-…`/`Bearer …` 一律打码)、`x-request-id`、端点路径、接口协议与是否流式(`proto=responses+sse`),全部进状态栏与诊断日志。502 不再只是一句"暂时不可用"。
- 连通测试镜像真实传输:开启流式预览时测试也走 SSE(结果行标注"流式/缓冲"),消除"测试通过但实际请求全 502"的盲区。
- 测试失败自动定位(最多 2 次小请求,仅在你点"测试"失败后触发):①同协议改缓冲重试——若通过,自动把该 Provider 标记为不支持流式(保存后生效);②/v1/responses 失败时用同网关 chat/completions 对照——直接告诉你是 responses 路由/模型通道映射的问题还是网关整体异常。
- 翻译中的 5xx 错误消息按协议给出可行动指引(提示做协议对比诊断),不再误导为纯偶发故障。

### 暂停/恢复大幅去摩擦

- 暂停分四类:用户主动(Esc/按钮)、人工修改横幅、写入中断事故、系统性暂停。只有前三类需要显式处理;**系统性暂停全部自愈**——继续输入新中文、重新定位成功或切换自动化模式即自动恢复,不再要求点"恢复"。
- 覆盖场景:会话/路由切换、输入框被外部清空、不确定发送、清空草稿残留、恢复历史草稿、保留人工英文之后——以上全部自愈。
- 另一窗口修改配置:从"暂停等待人工核对"改为**采纳并继续**(在途请求照旧作废、能力缓存照旧重置、凭据代次校验不变);读取失败仍然暂停。
- 用户主动暂停(Esc/按钮)语义不变且更严格:输入文字不会解除;Esc 在已暂停时把任何软暂停升级为用户暂停。
- 保留人工英文后继续输入:自动翻译恢复,但**人工文本永不被静默覆盖**——新英文停在预览,状态栏给出一键"同步到 Claude"(带确认)的明确指引。
- 定位按钮点击成功可同时解除写入中断类暂停(点击本身就是人工核对动作)。

### 评审门改为默认非拦截

- 纠错/歧义/数量类提醒不再阻止自动同步:英文照常写入 Claude 输入框(插件从不代发送),提醒在侧栏醒目保留,发送前自行过目。这是"有时不自动同步"的根因。
- 新增设置"严格评审"(默认关)可恢复旧行为:有提醒时英文停在预览,等你显式点"同步到 Claude"。
- 硬校验失败(占位符损坏、疑似未翻译回显、极性反转、越界纠错)不受影响,依旧直接报错不同步。

### 测试

- 新增 4 项 Node 测试(网关证据脱敏与协议/传输标记、401 不留正文、连通测试传输镜像、严格评审设置规范化),共 92 项。
- 浏览器状态机套件新增:系统暂停随输入自愈、Esc 暂停不被输入解除;外部配置采纳断言更新为"不强制恢复"。

## 0.2.10 — 2026-07-20

体验收敛版。基于外部 UX 设计评审报告,采纳其 P0 全部六项与若干可达性/成本可见性小项;拒绝大重构类建议(结果卡 Tab 化、设置向导、panel/writer 文件拆分、状态机重写)——对刚经五轮审查稳定的代码,重构风险与单人工具收益不成比例;"精确结果缓存"已在 0.2.8 实现。安全边界零削弱。

### 副作用如实表达

- 主按钮改名"翻译并同步"/"翻译整段并同步":按钮文案覆盖真实副作用(会请求模型并写入 Claude);底部快捷键说明同步更新。
- 回译方式三个选项标注请求次数(1 次/2 次),成本在选择时可见。
- 字数统计改为双上限表达:`n / 50,000`(可翻译),悬停提示 250,000 暂存上限;超出时明确标注"(超出,需拆分)"。

### 自动化改为明确三态

- "自动"开关升级为三态选择:手动(不自动请求)/自动翻译(停顿后请求模型但不写入 Claude)/自动翻译并同步(原行为)。
- 旧配置迁移:autoSync=true → 自动翻译并同步,false → 手动;storage 同时维护派生的 autoSync 布尔以兼容旧版本读取。
- 切换即时生效并给出模式含义说明;防抖触发时按触发时刻的模式决定是否写入。

### 单向 Esc

- Esc 只能暂停/取消,不再切换恢复:紧张时连按不会意外重启自动写入;恢复只能点"恢复"按钮。状态栏与页脚明确提示。

### 人工修改保护成为默认

- 人工编辑冲突卡重排:"保留 Claude 当前英文"为主按钮;"重新翻译并覆盖…"降为危险样式并新增执行前确认;文案改为保护性表述。

### 可达性

- 全部交互元素补 `:focus-visible` 可见焦点;高频按钮最小高度 36px。

### 测试

- Panel 状态机新增断言:第二次 Esc 不得恢复自动化(恢复必须经按钮);计数器双上限格式回归。88 项 Node 测试与 4 个 Chromium 套件全部通过。

## 0.2.9 — 2026-07-20

思考强度兼容层(v1)。基于外部《思考强度兼容层最终研究报告》,采纳其核心架构,裁剪其注册表/自动升级/自定义 DSL 部分(理由见 README 与提交说明)。

### 新增(lib/reasoning.js)

- 思考策略:继承接口默认(不发送,老配置零变化)/ 明确关闭 / 手动档位(minimal…max)。
- 思考参数方言,独立于接口协议:不发送(未知反代安全默认)、OpenAI Chat(顶层 reasoning_effort)、OpenAI Responses(reasoning.effort)、OpenRouter(规范嵌套形式)、DeepSeek(thinking.type + 档位映射 high/max + 思考模式移除采样参数)、通用 thinking.type(GLM 等)、通用 enable_thinking(Qwen 等,仅开关并明示档位不适用)。
- 构造性不变量:写入前剥离全部已知思考别名 ⇒ 不会同发 reasoning 与 reasoning_effort;关闭时不残留 effort/budget;inherit 不发送任何字段;每请求只有一个控制来源。
- 严格语义:接口以 400/422 明确拒绝思考参数(error.param 精确匹配)时,报 `reasoning_rejected` 并原样呈现,不进入兼容降级重试(实测网络调用恰一次)、不静默移除、不付费穷举其他字段。
- 证据(仅非敏感用量):从两种协议的 usage 提取 reasoning_tokens,展示在诊断记录与连通测试结果;思考内容本身不持久化、不进编辑器、不进日志。
- 设置 UI:策略/方言/档位三个选择 + 本地零费用映射预览(显示将发送/将移除的字段与映射说明);方言列表随接口协议过滤;Provider 摘要显示思考状态。
- 存储:reasoning 配置随 Provider 规范化(枚举白名单),参与外部窗口变更检测;显式排除在凭据绑定之外——调整思考策略不会使已存 Key 失配。

### 明确不采纳(相对研究报告)

- auto 模式与"验证失败自动升级"状态机:隐性二次计费,审阅门禁已兜底,后续按需求再议。
- 能力档案注册表(source/confidence/过期/端点指纹)与能力测试按钮:方言预设随扩展版本发布,服务端校验即真相。
- 自定义路径 DSL 适配器与 Anthropic/Gemini 原生协议:攻击面与复杂度不成比例,留待真实需求。
- 报告要求的 P0 前置(审阅门禁、数值/语言硬校验、Responses 严格化、暂停代际、还原后长度)已在 v0.2.7/0.2.8 全部完成。

### 测试

- Node 测试 82 → 88 项:各方言 inherit/off/manual 编码与别名剥离、DeepSeek 映射与采样冲突移除、协议不匹配安全跳过、拒绝不重试(恰一次调用)、双协议请求携带与用量提取、存储规范化与凭据绑定不变。

## 0.2.8 — 2026-07-20

速度优化版。参照 OpenAI 官方延迟优化指南与沉浸式翻译、kiss-translator、openai-translator 等开源项目的通行做法。

### 少发请求（零风险,默认生效）

- 规范化等价的原文变化(尾随空格/空行)不再触发付费重译:修订号保持稳定,已有英文与在途请求继续有效;translateNow 的原文快照守卫改为规范化比较。
- 会话级翻译缓存(LRU 50 条):键含规范化 Base URL、接口协议、模型、回译模式、保护词、字面重译集与规范化原文。撤销输入、来回修改、恢复草稿、重复短语即时回放,不再计费;纠错/歧义/警告随结果一起回放,审阅门禁照常生效。缓存命中无需 Key/权限检查;Provider 或设置变化时整体清空。

### 流式预览（体感提速,默认开启,可在设置关闭）

- Chat Completions 与 Responses API 均支持 SSE 流式:生成过程中英文预览逐步显示,不再盯着"正在翻译…"等完整响应。
- 安全模型不变:写入 Claude 的最终文本仍然只来自完整接收、完整解析、完整校验的响应;流式只改变预览显示。Chat 流按增量重组为标准 payload 后走与缓冲路径完全相同的严格校验(finish_reason 白名单、角色、refusal、工具调用);Responses 流以 `response.completed` 携带的完整对象走既有校验。
- 增量 JSON `english` 字段提取器处理跨分块的转义(\n、\"、\uXXXX)与占位符:预览中的受保护内容即时还原,半个 token 不闪现;校验重试会重启预览。
- 网关兼容:返回普通 JSON(忽略 stream 参数)自动回退,无额外请求;显式拒绝 `stream` 参数走既有逐项降级(capabilities.streaming),仅参数名精确匹配,不受错误消息中 "upstream" 之类字样影响;流式响应沿用 128KB 上限与超时。
- 每个流式事件的逻辑错误、空流、未收到完成事件分别分类;设置新增"生成过程中流式显示英文预览"开关。

### 未采纳项（经评估）

- Predicted Outputs:仅 OpenAI 端点支持,网关 400 风险高、仅利好"小改重译"场景(已被缓存与等价跳过覆盖大半),暂不实现。
- 长文分段并行:跨段术语一致性与占位符跨段处理复杂,典型短提示词场景收益有限,暂不实现。

### 测试

- Node 测试 77 → 82 项:Chat/Responses SSE 流式重组、忽略 stream 的透明回退、stream 参数拒绝的精确降级(JSON 模式存活)、提取器跨分块转义与半 token 隐藏、translateDraft 流式端到端。
- Panel 状态机新增两项速度回归:尾随空格不计费、缓存命中不计费。

## 0.2.7 — 2026-07-20

外部深度审计核验修复版:对第三方 AI 审计的 33 项发现逐条对照代码验证。31 项落实修复(其中数量保真、复述判定、路径识别 3 项为部分修复并文档化剩余边界);2 项(中文数词短语的语义判定、旧草稿跨窗口迁移原子性)接受为文档化已知边界;个别子例经验证为已有防护覆盖。

### 安全与状态机（panel.js / sw.js / lib/storage.js）

- 【高】暂停原因隔离:所有非目标丢失的暂停点(共 14 处)显式清除 `pausedByTargetLoss`,自动重绑成功不再可能误清外部配置审查、人工修改等其他暂停。
- 【高】审阅门禁:纠错、歧义或数量/复述/回答式/语序类警告存在时不再自动同步;英文保留在预览,用户点击"同步到 Claude"即显式批准。独立回译照常调度。
- 【高】SW 写命令在前台检查 `await` 前快照 lease/boundTabId/bindGeneration,恢复后全部一致才放行,并以快照 lease 发送;等待期间解绑+重绑不再让旧命令借新租约执行。
- 【高】配置保存回滚前,每个密钥槽位校验当前记录的凭据代次属于本事务/快照/为空,另一窗口保存中途写入的新 Key 不再被回滚覆盖(不可判定时失败关闭)。

### 翻译校验（lib/validation.js / lib/translator.js）

- 【高】纠错拒绝扩展:极性/方向/状态单字(是否开关多少前后真假对错增减升降高低上下左右内外正反新旧好坏快慢)与大写/异体数字(壹贰叁肆伍陆柒捌玖拾佰仟廿卅);超过 8 条的纠错列表整体越界;歧义超过 6 条产生显式警告。
- 【高】未翻译中文残留 ≥60% 从警告升级为硬失败("你好 a"不再通过);回译中文占比过低(≥8 字原稿 <20%)硬失败;"x中"式回译不再有效。
- 【高】中文数词数值核对:解析两字以上中文基数词(三十、一千二百、五万),与译文中的阿拉伯/中文数字数值比对,不一致产生数量警告并触发审阅门禁。
- 【高】占位符还原后英文/回译超过 100,000 字符硬失败,不再触发存储静默截断导致的状态失配。
- 双向覆盖/隔离符与行间注控制字符(U+202A-202E、U+2066-2069、U+FFF9-FFFB)一律硬拒绝。

### Provider 协议（lib/provider.js）

- Responses 解析严格化:对象标记存在即必须为 `response`;顶层仅 `completed`(或缺省)算成功,`in_progress/queued` 判未完成;消息项要求 assistant 角色与 completed 状态。
- Chat Completions:`finish_reason` 白名单(stop/end_turn/eos/缺省),非 assistant 角色拒绝,移除 legacy `choices[0].text` 回退。
- 兼容降级参数别名移除裸 `text`,无关 400 不再触发去 JSON 重试;404/405 文案协议中立。
- `/models` 返回非模型列表结构显式报 `unsupported_response_schema`,不再显示为"没有模型"。
- 密钥样式请求头拦截扩展(access-key、credential、signature、bearer、session-id、独立 key/auth/sign 词段),storage 侧同步;自定义鉴权 Header 槽位不受影响。
- 诊断端点路径改为默认脱敏:仅保留常见路由词、版本号与短纯字母段。

### Writer（writer.js）

- 写入/清除类命令执行前重新运行完整可编辑判定,隐藏/禁用/只读控件返回 `composer_not_editable`。
- 周期扫描在编辑器仍有效时也调度重排名,属性级 hydration 出现的更优主编辑器可被接管(+60 滞后不变)。
- 附件失败关闭覆盖仅带 aria-label 附件语义的兄弟节点芯片;按钮等交互控件排除,工具栏"添加附件"不会误触发。

### 占位符保护（lib/placeholders.js）

- Markdown 围栏改为逐字符扫描:开栏 3+ 个反引号/波浪线,闭栏必须不短于开栏(````…````不再被内部 ``` 提前闭合);行内代码支持等长多反引号(``a`b``)。
- Unix 路径识别扩展分隔上下文(：、=、[、,、;等)并支持 `~/`、`$VAR/` 前缀。
- 保护词先去重/去空再取 500 上限;逐码元推进以覆盖重叠出现。

### 文档与流程

- README 安装/升级说明去除固定 v0.2.2 目录名;BUILD_REPORT、TESTING 更新到当前版本并覆盖接口协议与浏览器套件说明。

### 测试

- Node 测试 66 → 77 项,覆盖上述全部可单测修复;4 个 Chromium 套件保持通过(独立回译 mock 更新为真实中文以匹配新的回译比例下限)。

## 0.2.6 — 2026-07-20

Responses API 支持与使用稳定性修复版（针对用户反馈的四类问题）。

### OpenAI Responses API（/v1/responses）

- 设置新增"接口协议"选项：Chat Completions（默认）或 Responses API。协议是显式选择,插件仍不自动猜路由、不跨协议重试。
- Responses 请求：system 提示词映射到 `instructions`,用户内容作为 `input_text` 输入项,`store: false` 退出服务端响应留存;JSON 模式使用 `text.format`。
- Responses 解析:遍历 `output` 数组(忽略 o 系列 `reasoning` 项),`output_text` 拼接;`refusal` 部件 → 拒绝,`incomplete/max_output_tokens` → 截断,`function_call` 等工具项 → 硬拒绝;`failed` 状态归类为逻辑错误。
- 协议错配双向可判定:Responses 模式收到 Chat Completions 结构(或反之)会提示到设置中切换协议。
- 兼容降级把 `text.format` 参数拒绝映射到统一的 JSON 模式能力位,`temperature` 独立降级;能力签名纳入协议维度,切换协议不会误用另一协议学到的降级。
- Base URL 规范化支持粘贴完整 `/responses` 地址;裸 `/responses` 收到 HTML 时同样提示可能缺少 API 前缀。

### 使用稳定性（用户反馈修复）

- 【回归修复】"立即翻译"按钮不再可能永久禁用:0.2.5 给按钮恢复加身份守卫后,任何经 `abortInFlight` 的中止(暂停、换绑、清空草稿、SW 重启)都会跳过恢复;现在中止即恢复按钮。
- 空输入框定位:新增 1 秒级低频重扫。Claude 空态编辑器经属性级 hydration 变为可定位时(childList 观察器看不到),不再需要用户先往页面输入内容。
- 普通换会话不再强制暂停:仅当插件拥有的文本被换会话孤立时才暂停;在途翻译不再被中止(写入侧本就有身份校验,中止只会浪费已计费请求),英文预览保留并提示可手动同步。
- 可恢复的目标丢失(页面刷新、MV3 服务工作线程重启)自动重绑成功后自动恢复运行,不再每次都要人工点"恢复";用户主动暂停不受影响,仍需手动恢复。

### 测试

- Node 测试 62 → 66 项:Responses 请求构建与解析、text.format 降级、拒绝/截断/工具调用/协议错配、存储协议字段规范化与 URL 后缀剥离。
- Writer Chromium 冒烟新增属性级 hydration 定位场景;Panel 状态机新增换会话不暂停、中止后按钮恢复、目标丢失自动恢复三个场景。

## 0.2.5 — 2026-07-20

深度缺陷审查修复版（四路并行审查 + 逐项人工验证，共修复 1 高危 / 5 中危 / 7 低危）。

### 翻译校验（lib/validation.js）

- 【高危】拒绝涉及中文数字与序数的"明显错字"纠错（如 三十→五十、第三→第五）。此前中文数字是普通汉字码点，能通过短片段形状校验，且 ASCII 数字软警告也不会触发，模型声称的纠错可以静默改变数量后直接同步进 Claude。
- 修复"回答式措辞"软警告的正则：`sure[,!]` 后误置的 `\b` 要求标点后紧跟单词字符，导致 "Sure, …"/"Sure!" 永远不匹配；否定词黑名单补充"莫"。

### 发送确认与绑定（writer.js）

- 真实发送导致编辑器被卸载、替换节点尚未挂载时，受信的 Enter 发送意图不再因 targetEpoch 递增而失效：发送仍被确认归档，而不是降级为伪 TARGET_CLEARED（草稿不归档、面板误暂停）。
- 普通 Enter 只在文档末尾插入换行（尾部代码块/列表,ProseMirror 在 keydown 阶段吞掉原生 input 事件）时,通过原始序列化对比取消发送意图；随后的清空/切换会话不再被伪造成已发送并清空中文草稿。
- attach 时对齐 lastUrl 并静默轮换会话：解绑期间发生的 SPA 导航不再让绑定后的第一个写入命令必然失败于 writer_session_changed 并强制暂停。
- 手动绑定切换到另一个空编辑器、旧编辑器文本原封未动时,不再伪造"输入框被外部清空"事件与暂停。

### 服务工作线程（sw.js）

- 换绑失败（not_claude / tab_unavailable）时释放旧标签页的租约与所有权。此前面板已按"完全解绑"处理（tabId 置空,幽灵事件过滤器失效）,而 SW 仍把旧 writer 的 SEND_CONFIRMED 等事件转发给面板,可触发错误的草稿归档。

### 侧栏面板（panel.js）

- 暂停→恢复（Esc、暂停按钮、自动开关）与取消的 IME 组合不再对"未变化且已翻译"的草稿重复发起付费请求,也不再用新生成的英文覆盖已核对、已同步的英文。
- 被 Ctrl+Enter 取代的旧翻译请求不再提前恢复"立即翻译"按钮（重复请求锁在新请求在途期间失效）；"缺少主翻译模型"提前返回路径补充按钮恢复。
- 诊断写入/清除接入目标状态机：写入成功后不再保留绿色"已同步"徽章与人工修改横幅（横幅上的"作为核对基线"会把诊断短句当作人工英文采纳）,清除成功后徽章复位。
- 旧存储中早于当前校验规则的 Base URL（如带 query 参数）不再让"保存设置"永久失败：无法规范化的旧 URL 视为无旧权限模式,不再阻塞撤销/保存流程。
- 运行时学习的兼容能力降级不再被普通设置保存持久化：保存只写入存储中已有的能力快照,显式连通测试结果仍可覆盖——与 0.2.2 文档承诺一致。

### Provider（lib/provider.js）

- claude.ai 禁用检查覆盖 FQDN 尾点形式：`https://claude.ai./v1` 不再绕过"Claude 站点不能作为 Provider"的硬禁止。

### 测试

- Node 测试扩充到 62 项：中文数字纠错拒绝、Sure 开头回答式警告、尾点 claude.ai 拒绝、换绑失败释放租约与幽灵事件阻断。
- Writer Chromium 冒烟新增 4 个回归：无替换卸载后的发送确认、尾部换行意图取消、解绑期导航后的静默会话轮换、手动换绑不伪造外部清空。已验证新场景在修复前代码上确实失败。

## 0.2.4 — 2026-07-19

针对 Provider 伪成功响应与 Claude 编辑器定位/手动绑定的热修复版。

### Provider 协议诊断

- HTTP 200 HTML、空正文、非 JSON、200 逻辑错误、Responses API、未知 JSON、空 `choices` 与空 assistant 内容分别分类，不再统一显示 `empty_response`。
- 设置页“检测模型”和“真实连通测试”直接显示安全协议摘要，包括 HTTP 状态、Content-Type、响应大小、请求 ID、端点和顶层结构。
- 裸 `/chat/completions` 或 `/models` 返回 HTML 时给出“可能缺少 API 前缀”的诊断提示，但不自动尝试 `/v1` 或重复请求。
- 恢复 `redirect: "error"`，并使用内部 tagged result，远端 JSON 无法伪造本地协议错误通道。
- 诊断元数据经过白名单与脱敏，不保存 API Key、Authorization、提示词、请求正文、完整 HTML 或完整远端错误内容。

### Claude 输入框与手动绑定

- 输入语义扩展到父级 `aria-label`、`aria-placeholder`、`data-placeholder`、`data-testid` 等属性。
- 空输入状态下 disabled 的真实发送按钮仍作为定位证据；隐式 submit 工具按钮不会被误当作发送按钮。
- 支持短内层 Lexical/ProseMirror/plaintext-only 编辑节点，并在后续 hydration 出现更优候选时重新排名。
- 历史消息行内编辑器降权；已验证的手动目标不会被普通自动重排抢占。
- 手动绑定使用 `composedPath()`，等待 React/Lexical 重渲染并验证稳定性；页面导航、租约变化、detach 或新请求会取消旧的异步验证。
- 同一节点再次手动绑定也会刷新 Writer 状态，成功结果必须携带 `composerReady`。

### 测试

- Node 测试扩充到 58 项，覆盖 HTML/空正文/非 JSON/结构不兼容、诊断脱敏、远端 marker 注入和禁止重定向。
- Writer Chromium 冒烟新增父级语义 + disabled 发送按钮、React 节点替换和绑定期间 detach 取消回归。

## 0.2.3 — 2026-07-19

深度缺陷审查修复版。完整问题清单与验证状态见 [BUGCHECK_REPORT.md](BUGCHECK_REPORT.md)。

### 发送确认与清除抑制（writer.js）

- 修复自动清除失败后遗留的写入抑制状态可能无限期吞掉真实发送的问题：失败路径恢复或解除抑制，发送意图只在 DOM 真正被本插件清空后才作废；非受信回声抑制不再匹配空文本。
- SPA 导航后保留未变化的插件草稿所有权：`previouslyOwned` 重认领比较改用导航前的 `lastWritten`，同节点分支同样恢复所有权,不再把插件文本误判为人工内容。
- 普通 Enter 之后的受信删除/撤销输入会取消键盘发送意图,防止“无效 Enter + 3 秒内手动清空”被伪造成已发送并归档。
- 输入框被覆盖层短暂隐藏时保留绑定并宽限重查（上限 5 秒），不再把仍然存在的文本误报为外部清空。
- 写入命令在“发送清空后、reconcile 前”到达时改走 `confirmClear`,由发送意图决定归档,不再以空文本报人工修改。
- 发送按钮判定不再把表单内隐式 `type="submit"` 的工具按钮当作发送控件。
- 发送意图 keydown 监听移到 document 捕获阶段,先于页面编辑器自身的 Enter 处理执行。
- 文本控件（textarea/input）目标增加轮询,页面程序化清空同样能确认发送。
- 附件失败关闭检测扩展到编辑器容器内的兄弟节点（仅识别明确的附件标记属性）。
- 预渲染文档推迟到激活后才连接写入端口。

### 服务工作线程（sw.js）

- 新增 `tabs.onAttached`：绑定的标签页拖到其他 Edge 窗口后写入不再永久失败于“不在前台”。
- 新增 `tabs.onReplaced`：标签页被浏览器替换时清除失效绑定并通知面板。
- 拒绝 `documentLifecycle` 非 `active` 的写入端口连接,预渲染文档不能抢占 writer 槽位或继承租约。
- 面板端口注册校验发送方：来自标签页上下文的 `zh2en-panel` 连接一律拒绝。
- 初始化失败不再留下未处理拒绝,工具栏按钮仍可打开侧栏展示面板自身的失败关闭错误。

### 侧栏面板（panel.js）

- 暂停或人工修改状态下的显式同步不再被静默丢弃,状态栏明确提示；关闭回译模式下“正在准备同步…”不再卡死。
- 重新绑定不再无条件把目标标记为“已同步”：仅当 Claude 中内容与当前中文修订的英文一致才显示绿色徽章,过期内容恢复旧译文警示并重排自动清除。
- 目标标签页关闭/被接管时重置 `pluginOwned`/`composerReady`,消除“幽灵所有权”导致的错误横幅。
- 设置对话框已打开时的重复 `openSettings()` 变为幂等,不再重置表单、吞掉在途检测请求或抛 `InvalidStateError`。
- “立即翻译”直接中止独立回译时同步重置回译阶段,徽章不再卡在“等待回译”。

### Provider 与存储（lib/）

- Base URL 校验拒绝空查询/空片段分隔符（`…/v1?`、`…/v1#`）,此前会生成永远 404 的端点。
- Header 值、鉴权前缀与 API Key 校验扩展到拒绝非 Latin-1 字符,避免 `fetch()` 抛错被误诊为“网络错误”。
- 超过 128KB 的错误响应体按 HTTP 状态分类（保留 429 的 `Retry-After` 与 401 的提示）,不再被 `response_too_large` 掩盖。
- 凭据绑定串的 Header 排序改为码元序,不再受语言环境/ICU 版本影响。
- 凭据绑定串超限时在保存阶段显式失败,不再出现“保存成功但 Key 永远读不出”。
- 配置保存回滚前校验当前存储归属,不再覆盖另一窗口已提交的保存。
- `minimum_chrome_version` 提升到 118（`storage.local.setAccessLevel` 的最低要求）。

### 测试

- Node 测试扩充到 57 项：新增 URL 分隔符、非 Latin-1 Header、大错误体分类、绑定排序确定性、预渲染拒绝、跨窗口移动标签页与标签页替换回归。
- Chromium 冒烟与状态机安全测试全部通过。

## 0.2.2 — 2026-07-19

### Provider、Key 与多窗口一致性

- API Key 改为带版本的 Secret envelope，并与规范化后的 Provider 目标及 `credentialId` 强绑定。
- 请求读取 Key 时同时验证当前存储 Provider、Base URL、鉴权 Header、鉴权前缀、路由 Header、Key 存储模式和凭据代次。
- 修复窗口 A 缓存旧 Provider、窗口 B 保存新 Key 后，窗口 A 可能把新 Key 配到旧请求目标上的竞态。
- 旧版裸字符串 Key 仅在 Provider 仍为旧格式时兼容读取；下一次明确保存自动迁移为绑定记录。
- 删除运行时无 Provider 绑定的 Secret 读取/写入导出，翻译、模型检测和连接测试必须使用绑定读取。
- 另一个窗口改变 Provider、模型、Key 或保存方式时，当前窗口取消请求、作废运行上下文并自动暂停。
- 当前窗口自身保存产生的存储事件通过精确签名和凭据代次识别，不会误触发外部修改警告。
- 行为设置保存不再连带覆盖 Provider；运行时能力降级默认只更新当前上下文。

### Provider 兼容与请求边界

- 自定义 Header 名称按大小写不敏感去重，避免重复语义 Header。
- 若网关依次拒绝 `response_format` 和 `temperature`，允许最多两次、逐项且有界的兼容降级。
- 任意无关 400/422、模型错误或业务错误不触发兼容重试。
- 保留工具调用、函数调用、结构化拒绝、非文本内容、截断、过滤、空响应和超限响应的硬拒绝。

### Claude 编辑器兼容

- 输入框选择器新增 `contenteditable="plaintext-only"`。
- 继续覆盖 textarea、文本类 input、常规 contenteditable、SPA 会话切换、附件保护、事务回读与条件回滚。

### 测试与审计

- Node 单元/集成测试扩充到 52 项。
- Panel Chromium 状态机新增外部 Provider/Key 变化暂停与当前窗口自保存不误暂停回归。
- Writer Chromium 测试新增 `plaintext-only` 编辑器定位与写入。
- 静态审计新增凭据代次、Provider 绑定、无绑定 Secret API 禁止和跨窗口配置失效不变量。

## 0.2.1 — 2026-07-19

### 标签页、会话与 Writer

- 修复快速连续绑定两个 Claude 标签页时，较慢的旧 `tabs.get()` 结果可能回绑旧标签页的问题。
- Writer 在每个状态型命令前同步检查 URL；`history.pushState()` 静默换会话也会使旧 Writer Session 失效。
- 普通 Enter 真正插入换行时取消临时发送意图，避免随后手动清空被误判为发送。
- 新增附件、图片、文件卡片、mention 和有意义的原子富文本节点保护；写入/清理失败关闭。
- 附件在事务中途出现时，不执行可能删除附件的二次全选回滚。
- 修复“确认覆盖并同步”被人工编辑/暂停守卫无条件拦截的问题。
- 修复清空侧栏草稿时覆盖 Writer 恢复失败或人工修改警告状态的问题。

### 翻译与回译校验

- 用户原文中类似插件占位符的普通文本不再触发未知令牌硬失败。
- 短中文必须产生真实英文；只有原文为中文数词时才接受纯数字结果。
- 同请求回译不含中文时改为硬失败；独立回译可受控重试一次。
- `corrections` 和 `ambiguous` 非数组时拒绝结果。
- 修复随机占位符中的数字造成错误数字差异警告的问题。
- 保护内容恢复后再执行回译语言和数字检查。
- 独立回译同样保护 URL、代码、路径和保护词，且请求中仍不包含中文原稿。

### Provider 与配置

- 只有明确不支持 `response_format` 或 `temperature` 时才降级一次；任意 400 不再重复请求。
- HTTP 404 区分模型不存在/无权限与端点路径错误。
- 拒绝结构化 refusal、工具/函数调用、非文本内容 part、截断、过滤和异常大响应。
- 载入旧 Provider 设置时清除 Cookie、Authorization、Token、Secret、内容控制头和危险对象键。
- 无效鉴权 Header 或含控制字符的前缀在运行前回退到安全默认值。
- Provider、设置、Secret、草稿、历史和诊断的存储结构统一限长、限量和类型规范化。

### 草稿与错误恢复

- 发送归档、不确定发送归档和清空草稿的异步存储失败现在会记录并提示，不再产生未处理 Promise rejection。
- 活动中文草稿存储上限提高到 250,000 字符；翻译上限仍为 50,000，避免超长未译草稿关闭侧栏后被截断。
- 清理 Claude 失败后，侧栏可以清空自身草稿，但保持自动同步暂停并明确提示 Claude 仍有内容。

## 0.2.0 — 2026-07-19

- 默认由一次 Chat Completions 请求同时返回 `english` 与 `back_translation`。
- 保留独立第二次回译和关闭回译模式。
- 修复立即翻译后防抖重复请求、独立回译模型未测试、多窗口草稿互相覆盖和 Key 存储模式切换事务问题。
- 增强兼容网关 JSON 提取、工具调用拒绝、回译版本标记及 JSON 扫描上限。

## 0.1.1 — 2026-07-19

- Writer 改为事务式写入、回读、条件回滚。
- 加入标签页租约、Provider Epoch、Target Epoch、可信发送识别、草稿恢复和存储访问隔离。
- 增强 Header、响应大小、占位符、明显错字和错误处理边界。

## 0.1.0 — 2026-07-19

- 首个可侧载 MVP。
